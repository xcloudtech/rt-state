import * as React from 'react';
import { DemoHooks } from '../demo/demo-hooks';

export default { title: 'demo-hooks' };

export const Hooks = {
    name: 'DemoHooks',
    render: () => <DemoHooks />,
};
