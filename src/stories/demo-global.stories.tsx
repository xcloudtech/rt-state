import * as React from 'react';
import { DemoGlobalStateAndWatcher } from '../demo/demo-global';

export default { title: 'demo-global' };

export const GlobalStateAndWatcher = {
    name: 'DemoGlobalStateAndWatcher',
    render: () => <DemoGlobalStateAndWatcher />,
};
